org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# --- Local toolchain -------------------------------------------------------
# This machine has no system-wide JDK on PATH (only a JRE), so the JDK that
# Gradle/AGP must compile with is pinned here. Delete this line if you set a
# proper JAVA_HOME pointing at a JDK 17+ installation.
org.gradle.java.home=D:/android-toolchain/jdk21
